# Ventana embebida para ./scripts/run_desktop.sh (Qt WebEngine vía pip; sin Chromium del sistema).
# Tamaño aproximado: PySide6 ~150–250 MB en disco según plataforma.
#
# WSL: ./scripts/run_desktop.sh abre el navegador de Windows si no hay DISPLAY/Wayland (típico).
#      PySide6 solo hace falta si quieres ventana embebida con WSLg (Windows 11).
pywebview>=5.0
qtpy>=2.4.0
PySide6>=6.5.0
